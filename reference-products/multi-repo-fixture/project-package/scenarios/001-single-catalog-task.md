# Scenario: Single catalog task

Create a single task with labels:

- `trigger:auto-implement`
- `repo:catalog`

Task body:

Add a catalog summary field named `inactive` that counts unavailable items. Update catalog-service tests. Do not modify storefront.
