# TypeScript API and Web Implementation Skill

Work in the product repository next to this package.

Rules:

- Keep shared scheduling behavior in `src/domain`.
- Keep API wiring in `src/api`.
- Keep Web UI entrypoints in `src/web`.
- Add or update Vitest coverage when behavior changes.
- Run `commands/verify.sh` before reporting completion.
- If a task requires changing A2O public behavior, stop and ask the project owner before proceeding.
