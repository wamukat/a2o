# Python Service Implementation Skill

Work in the product repository next to this package.

Rules:

- Keep appointment behavior in `src/a2o_reference_service/appointments.py`.
- Keep HTTP wiring in `src/a2o_reference_service/app.py`.
- Prefer the Python standard library unless the task explicitly justifies a dependency.
- Add or update unittest coverage when behavior changes.
- Run `commands/verify.sh` before reporting completion.
- If a task requires changing A2O public behavior, stop and ask the project owner before proceeding.
